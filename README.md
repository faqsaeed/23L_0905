# CS4048 Assignment 1 — Version C

1. The roll number is configured as `23L_0905` (Version C because `(905 mod 3) + 1 = 3`).
2. Install dependencies with `pip install -r requirements.txt` and ensure Google Chrome is installed.
3. Run all required notebook cells to regenerate both CSV files.
4. For the optional bonus, install the browser with `playwright install chromium` and uncomment its cell.
5. Publish the two generated CSVs to Kaggle or GitHub and replace the URL placeholder.
6. Rename the notebook/CSVs if necessary and zip the notebook, both CSVs, and URL text file as `Assignment1_<ROLL_NO>.zip`.

The notebook includes methodology, completion rules, retry behavior, validation statistics, and the optional Playwright comparison.
